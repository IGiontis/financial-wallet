// Vitest global setup — extends `expect` with jest-dom matchers.
// React Testing Library (v16) auto-registers DOM cleanup after each test
// when Vitest globals are enabled, so no manual afterEach is required here.
import "@testing-library/jest-dom/vitest";
